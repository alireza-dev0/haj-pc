export interface IProduct {
    id: string;
    name: string;
    price: number;
    stock: number;
    description: string;
    thumbnail: string;
    images: string[];
    createdAt: string;
    updatedAt: string;
}
